[build]
  command = "npm install && npm run build:shared && npm run build:web"
  publish = "packages/web/out"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
